# Frontend Local Development

From `OCMS-Frontend`:

```bash
npm run dev
npm run build
```

## Notes

- API calls go through `services/api.js`.
- Avoid hardcoding API hosts outside the shared API client.
- Use shared service modules for feature API calls.
